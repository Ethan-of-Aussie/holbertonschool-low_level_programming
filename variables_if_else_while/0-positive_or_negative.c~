#include <stdlib.h>
#include <time.h>
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* your code goes there */
	if(1 > 0)
	  printf("is positive")
	  if(0 == 0)
	    printf("is zero")
	    if( < 0)
	      printf("is negative")
		(/n);
	return (0);
}
