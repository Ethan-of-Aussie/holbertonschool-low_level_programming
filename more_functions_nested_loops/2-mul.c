#include "main.h"
/**
 *mul - entry
 *description: 'militiply the ints inputs'
 *@a: first input
 *@b: second input
 *Return: input and the 0
*/
int mul(int a, int b)
{
	int t = a * b;

	return (t);

	return (0);
}
