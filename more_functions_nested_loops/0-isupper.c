#include "main.h"
/**
 * _isupper - entry point
 *description:'to Make sure the input is a Captial letter and return 1'
 *@c: input.
 *Return: input and number return.
 */
int _isupper(int c)
{
	if ((c) >= 'A' && (c) <= 'Z')
	{
	return (1);
	return (c);
	}
	else
	{
	return (0);
	return (c);
	}
}
