#include "main.h"
/**
 *print_square - entry
 *description: 'function that prints a square, followed by a new line'
 */
void print_square(int size)
{
	int r, c;

	if (size <= 0)
	  {
	    _putchar('\n');
	  }

	for (r = 0; r < size; r++)
	  {
	    for(c = 0; c < r; c++)
	
}
