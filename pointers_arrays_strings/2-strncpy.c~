#include "main.h"

char *_strncpy(char *dest, char *src, int n)
{
	int i = 0;
	int a = 0;
	while (dest[i] != '\0')
	{
	i++;
	}
	while (a < n && src[a] != '\0')
	{
	dest[i] = src[a];
	a++;
	i++;
	}
	dest[i] = '\0';
return (dest);
}
