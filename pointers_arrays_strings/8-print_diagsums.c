#include "main.h"
#include <stdio.h>
/**
 *
 *
 */
void print_diagsums(int *a, int size)
{
	int i, j;
	
	for (i = 0; i < *a; i++)
	{
	for (j = 0; j < *a; j++)
	{
	_putchar(a);
	}
	_putchar('\n');
	}
}
