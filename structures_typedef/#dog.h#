#include <stdio.h>
#include <stddef.h>
#include <stdlib.h>
#include "main.h"
/**
 *
 *
 */
dog_t *dog(char *name, float age, char *owner)
{
	struct dog *d;
	int i;

	d = malloc(sizeof(dog_t));
	if (!d)
		return (NULL);
	d->age = age;
	for (i = 0; name[i] != '\0'; i++)
		;
	d->name = malloc(i + 1);
	
	return (d);
}
