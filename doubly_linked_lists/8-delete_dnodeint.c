#include "lists.h"
/**
 *
 */
int delete_dnodeint_at_index(dlistint_t **head, unsigned int index)
{
	unsigned int i;
	dlistint_t *cur;

	for (i = 0; i < index && cur != NULL; i++)

	if ( == 1)
	return()
	if ( == -1)
	return ()

return ();
}
