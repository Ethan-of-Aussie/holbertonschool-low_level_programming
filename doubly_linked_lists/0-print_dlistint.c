#include "lists.h"
/**
 *
 */
size_t print_dlistint(const dlistint_t *h)
{
	dlistint_t* head;
	dlistint_t* tail;
	dlistint_t* n;

newlist = malloc(sizeof(dlistint_t));
	if (!newlist)
	return(NULL);

  n = new dlistint_t;
  n->next = 1;
  n->prev = NULL;
  head = n;
  tail = n;

  while (h)
    
}
