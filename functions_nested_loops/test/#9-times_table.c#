#include "main.h"
/**
 *description:''
 *times_table - read code!
 *
 */

void times_table(void)
{
  int t;
	int a;
	int c;
	int j;
	int p = 0;
	for (c = 0; c <= 9; c++)
	  {
	for (j = 0; j <= 9; j++)
	      {
		for (t = 0; t <= 9; t++)
		  {
		a = c * j;

	if (a <= 9)
	  {
	    _putchar(a + '0');
	_putchar(',');
          _putchar(' ');
          _putchar(' ');
		  }

	else
	  {
	_putchar((a / 10) + '0');
	_putchar((a % 10) + '0');
	
          _putchar(',');
          _putchar(' ');
          
	  }

	      }
	while (p < 1)
	  {
	_putchar((a / 10) + '0');
        _putchar((a % 10) + '0');
	p++;
	  }
	      }
	_putchar('\n');	     
	;
	  }
}	
