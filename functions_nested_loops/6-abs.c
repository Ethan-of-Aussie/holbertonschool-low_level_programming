#include "main.h"
/**
 *_abs - check the code.
 *Desrciption: 'function that computes the absolute value of an integer.'
 *@c: is used for the input.
 * Return: Always 0.
 */
int _abs(int c)
{
	if ((c) < 0)
	return (-(c));
	return (c);
}
